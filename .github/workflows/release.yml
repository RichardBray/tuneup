name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Build all platform binaries
        run: bun run build:standalone

      - name: Generate checksums
        working-directory: dist
        run: sha256sum tuneup-macos-apple-silicon tuneup-macos-intel tuneup-linux-x64 tuneup-windows-x64.exe > checksums.txt

      - name: Create release with assets
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create "${{ github.ref_name }}" \
            dist/tuneup-macos-apple-silicon \
            dist/tuneup-macos-intel \
            dist/tuneup-linux-x64 \
            dist/tuneup-windows-x64.exe \
            dist/checksums.txt \
            install.sh \
            --title "${{ github.ref_name }}" \
            --generate-notes
